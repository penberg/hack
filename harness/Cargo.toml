[package]
name = "dwim-harness"
version.workspace = true
edition.workspace = true

[lib]
path = "lib.rs"

[dependencies]
dwim-models.workspace = true
dwim-tools.workspace = true

[dev-dependencies]
dwim-models = { workspace = true, features = ["testing"] }
