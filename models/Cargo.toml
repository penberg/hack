[package]
name = "dwim-models"
version.workspace = true
edition.workspace = true

[lib]
path = "lib.rs"

[dependencies]
fancy-regex = "0.19.1"
libc = "0.2.189"
dwim-gpu.workspace = true
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"

[dev-dependencies]
dirs = "7.0.0"

[features]
# The scripted model and the tiny tokenizer, for other crates' tests.
testing = []
